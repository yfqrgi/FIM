# File integrity monitoring

Calculates the SHA-256 hash of a file. Even if a single character or pattern in the file changes, the hash changes completely and the change is immediately noticeable.

### Run
```bash
python fim.py
